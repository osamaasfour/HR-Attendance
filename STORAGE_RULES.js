/**
 * FIREBASE STORAGE RULES — paste into Firebase Console → Storage → Rules
 * Deployable source: storage.rules
 *
 * NOTE: Cloud Storage requires the Blaze (pay-as-you-go) plan.
 * Until Storage is enabled, the app falls back to inline data URLs for photos/docs.
 */
