/**
 * FIRESTORE SECURITY RULES — paste into Firebase Console → Firestore → Rules
 * Deployable source of truth: firestore.rules
 */

/*
See firestore.rules in project root for the full ruleset.
Includes: users, companySettings, branches, departments,
attendance, hrRequests, leaveRequests, notifications,
compensation, loans, payrollSettings, payslips, payrollRuns
*/
