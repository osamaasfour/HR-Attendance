/**
 * Upload supporting documents for HR requests (e.g. sick leave)
 * Prefers Storage; falls back to inline data URL on Spark.
 */

export { uploadHrAttachment } from './uploadProfilePhoto';
export type { PickedDocument } from './uploadProfilePhoto';
